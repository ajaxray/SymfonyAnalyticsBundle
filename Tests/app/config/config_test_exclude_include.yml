imports:
    - { resource: config.yml }

analytics:
    exclude_patterns: ['/path/to/ignore/first', '/path/to/ignore/second']
    only_patterns: ['/path/to/include/first', '/path/to/include/second']
    watch_groups:
      - { name: 'Products', patterns: ['/product/view/\d*', '/product/list'] }
